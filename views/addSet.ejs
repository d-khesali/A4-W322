<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Sets!!!</title>
  <link rel="stylesheet" href="/css/main.css" />

</head>

<body tabindex="-1">

  <%- include('partials/navbar', {page: '/lego/addSet'}) %>

  <div class="container mx-auto p-4">

    <div class="hero bg-base-200 rounded-lg p-6">
      <div class="hero-content text-center ">
        <div class="max-w-md">
          <h1 class="text-4xl font-bold">Add Your Favorite Set!</h1>

        </div>
      </div>
    </div>

<div class = "mt-6">
    <form action="/lego/addSet" method="POST" class="w-full max-w-md mx-auto p-4 border border-black rounded-lg">
        <label for="name" class="block mb-2 text-black">Set Name:</label>
        <input type="text" id="setName" name="name" required class="w-full px-3 py-2 mb-4 border border-black rounded-lg">
      
        <label for="year" class="block mb-2 text-black">Set Year:</label>
        <input type="number" id="setYear" name="year" required class="w-full px-3 py-2 mb-4 border border-black rounded-lg">
      
        <label for="num_parts" class="block mb-2 text-black">Number of Parts:</label>
        <input type="number" id="setNum_parts" name="num_parts" required class="w-full px-3 py-2 mb-4 border border-black rounded-lg">
      
        <label for="img_url" class="block mb-2 text-black">URL to Set Image:</label>
        <input type="text" id="setImg_url" name="img_url" required class="w-full px-3 py-2 mb-4 border border-black rounded-lg">
      
        <label for="theme_id" class="block mb-2 text-black">Set Theme:</label>
        <select id="theme_id" name="theme_id" required class="w-full px-3 py-2 mb-4 border border-black rounded-lg">
          <% themes.forEach(theme => { %>
            <option value="<%= theme.id %>">
              <%= theme.name %>
            </option>
          <% }) %>
        </select>
      
        <label for="set_num" class="block mb-2 text-black">Set Number:</label>
        <input type="text" id="set_num" name="set_num" required class="w-full px-3 py-2 mb-4 border border-black rounded-lg">
      
        <button type="submit" class="w-full px-4 py-2 text-white bg-black rounded-lg hover:bg-gray-800">Add Set Now</button>
      </form>
    </div>



  </div>
</body>

</html>